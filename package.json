{
  "dependencies": {
    "@fastly/expressly": "^2.3.0",
    "@fastly/js-compute": "^3.30.1",
    "handlebars": "^4.7.8"
  },
  "devDependencies": {
    "@fastly/cli": "^10.14.0"
  },
  "scripts": {
    "build": "js-compute-runtime src/index.js bin/main.wasm",
    "start": "fastly compute serve --watch",
    "deploy": "fastly compute publish --domain=httpkeanu.edgecompute.app"
  }
}
